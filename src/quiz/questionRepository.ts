import type { QuizQuestion } from "./types";

export async function listQuestions(): Promise<QuizQuestion[]> {
  return [];
}

export async function getDailyQuestions(_dateKey: string): Promise<QuizQuestion[]> {
  return [];
}
